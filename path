# Configure custom paths. Default paths are set in /etc/paths.
export PATH=$PATH:${GOPATH//://bin:}/bin

# Load rbenv into a path.
if [ command -v rbenv &> /dev/null ]; then
  eval "$(rbenv init -)"
fi

